name: PR Sandbox

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  create-sandbox:
    # Skip forks - they don't have access to secrets or OIDC
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
      pull-requests: write
      packages: read
    outputs:
      sandbox_url: ${{ steps.sandbox-url.outputs.url }}
      sandbox_env: ${{ steps.sandbox-url.outputs.env }}
    env:
      AWS_REGION: us-east-1
      BUCKET_NAME: apps-ecosystem-artifacts-311141564186
      APP_NAME: app2
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://npm.pkg.github.com'

      - name: Install dependencies
        run: npm ci
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Run unit tests
        run: npm test

      - name: Build
        run: npm run build
        env:
          APP_REF: pr-${{ github.event.pull_request.number }}
          APP_SHA: ${{ github.event.pull_request.head.sha }}
          APP_ENV: sandbox-pr-${{ github.event.pull_request.number }}

      - name: Add build info
        run: |
          echo '{ "ref": "${{ github.event.pull_request.head.sha }}", "pr": ${{ github.event.pull_request.number }}, "built": "'$(date -Iseconds)'" }' > dist/build-info.json

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::311141564186:role/github-actions-deploy
          aws-region: ${{ env.AWS_REGION }}

      - name: Publish PR artifacts
        run: |
          REF="pr-${{ github.event.pull_request.number }}"
          aws s3 sync dist/ "s3://$BUCKET_NAME/artifacts/$APP_NAME/$REF/" --delete

      - name: Trigger sandbox creation
        env:
          GH_TOKEN: ${{ secrets.ECOSYSTEM_DISPATCH_TOKEN }}
        run: |
          PR_NUM="${{ github.event.pull_request.number }}"
          gh workflow run create-sandbox.yml \
            --repo aws-infra-example/infra-ecosystem \
            --field sandbox_id="pr-${PR_NUM}" \
            --field app_versions='{"app2": "pr-'${PR_NUM}'"}' \
            --field source_repo="${{ github.repository }}" \
            --field source_pr="${PR_NUM}"

      - name: Set sandbox URL
        id: sandbox-url
        run: |
          ENV="sandbox-pr-${{ github.event.pull_request.number }}"
          echo "url=https://d3qmcix5z43lko.cloudfront.net/${ENV}/" >> $GITHUB_OUTPUT
          echo "env=${ENV}" >> $GITHUB_OUTPUT

      - name: Comment on PR
        uses: actions/github-script@v7
        with:
          script: |
            const prNum = context.payload.pull_request.number;
            const sandboxUrl = `https://d3qmcix5z43lko.cloudfront.net/sandbox-pr-${prNum}/app2/`;
            const infraPrUrl = `https://github.com/aws-infra-example/infra-ecosystem/pulls?q=is%3Apr+sandbox%2Fpr-${prNum}`;

            github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: prNum,
              body: `## Sandbox Deploying

**URL:** ${sandboxUrl}

**Customize versions:** [Edit sandbox manifest](${infraPrUrl})

To test with specific versions of other apps, edit the manifest in the infra-ecosystem PR.`
            });

  e2e-tests:
    needs: create-sandbox
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read
    env:
      E2E_BASE_URL: ${{ needs.create-sandbox.outputs.sandbox_url }}
      E2E_ENV: ${{ needs.create-sandbox.outputs.sandbox_env }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://npm.pkg.github.com'

      - name: Install dependencies
        run: npm ci
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Wait for sandbox deployment
        run: |
          echo "Waiting for sandbox to be ready..."
          sleep 60

      - name: Run E2E tests
        run: npm run test:e2e

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-test-results
          path: |
            test-results/
            playwright-report/
          retention-days: 7
